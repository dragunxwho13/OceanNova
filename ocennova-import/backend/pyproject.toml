[project]
name = "oceannova"
version = "0.1.0"
description = "AI-Powered Hyperspectral Ocean Anomaly Intelligence"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
